replicas: 1

minimumMasterNodes: 1

resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    cpu: "500m"
    memory: "1Gi"

# отключаем pod anti-affinity, чтобы на одном узле всё поднималось
podAntiAffinity: "soft"
