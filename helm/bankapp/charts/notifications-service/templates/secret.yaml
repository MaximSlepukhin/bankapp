apiVersion: v1
kind: Secret
metadata:
  name: notifications-db-secret
type: Opaque
stringData:
  POSTGRES_USERNAME: "{{ .Values.secrets.POSTGRES_USERNAME }}"
  POSTGRES_PASSWORD: "{{ .Values.secrets.POSTGRES_PASSWORD }}"
  KEYCLOAK_CLIENTSECRET: "{{ .Values.secrets.KEYCLOAK_CLIENTSECRET }}"
