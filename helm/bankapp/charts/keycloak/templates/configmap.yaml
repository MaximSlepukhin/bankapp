{{- if .Values.realmImport.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-realm
data:
  {{ .Values.realmImport.file }}: |-
    {{ .Files.Get .Values.realmImport.file | nindent 4 }}
{{- end }}
